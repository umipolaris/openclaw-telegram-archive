name: Build Artifact

on:
  workflow_dispatch:

jobs:
  build-images:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build backend image
        run: docker build -t doc-archive-backend:latest backend
      - name: Build frontend image
        run: docker build -t doc-archive-frontend:latest frontend
